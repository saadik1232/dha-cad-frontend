import React from "react";

const Spinner = (props) => {
  return (
    <React.Fragment>
      <span>
        <i className="fa fa-spinner fa-spin"></i>
        &nbsp;
      </span>
    </React.Fragment>
  );
};

export default Spinner;
